<table class="table">
  <tr>
    <th>
    <%= attachment_image_tag user, :user_image, :fill, 100, 100, fallback: "No_image.jpeg" %>
    </th>
    <th>
      <%= link_to "編集", class:"btn btn-success" %>
    </th>
  </tr>
  
  <tr>
    <th>name</th>
    <th><%= user.name %></th>  
  </tr>
    <tr>
    <th>フォロー</th>
    <th>
      <%= link_to follower_user_path(user.id) do %>
      <%= user.follower_user.count %>
      <% end %>
    </th>  
  </tr>
  
  <tr>
    <th>フォロワー</th>
    <th>
      <%= link_to followed_user_path(user.id) do %>
      <%= user.following_user.count %>
      <% end %>      
    </th>  
  </tr>

  </table>
  
