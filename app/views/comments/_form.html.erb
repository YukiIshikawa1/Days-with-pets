<%= form_with(model: [post, comment], url: post_comments_path(@post) ) do |f| %>
  <div>
    <%= f.text_area :comment, placeholder: "コメントはここに", :size =>"50x5" %>
  </div>
  <div>
    <%= f.submit "送信", class: "btn btn-outline-dark comment-submit" %>
  </div>  
<% end %>