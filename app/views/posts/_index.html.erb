<% posts.each do |image| %>
  <div class="index-image">
    <div>
    <%= link_to user_path(image.user) do %>
      <%= attachment_image_tag image.user, :user_image, size:"100x100" ,class: 'rounded-circle shadow' %>
    <% end %>
    </div>
    <%= link_to post_path(image) do %>
      <%= attachment_image_tag image, :post_image, size:"600x400" ,class: 'border border-primary shadow' %>
    <% end %>  
    
  </div>
  
  <div class="title">
    タイトル：<%= image.title %>
  </div>
  
  <div class="text">
    本文：<%= image.text %>
  </div> 
  <div class="user">
    <%= link_to user_path(@user1.id) do %>
      by <%= image.user.name %>
    <% end %>
  </div>  
  
  <div class="favorite mb-5">
    いいね：<%= image.favorites.count %>  コメント：<%= image.comments.count %>
  </div>
  
<% end %>
<div class="paginate">
  <%= paginate  (posts) %>
</div>